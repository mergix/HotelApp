@model  IEnumerable<Booking>
@{
    ViewBag.Title = "Room";
}

<div class="container">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary"> Bookings List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Admin" asp-action="CreateBooking" class="btn btn-primary"><i class="bi bi-plus-lg"></i>
                New Room
            </a>
        </div>
    </div>


    <table class="table table-bordered table-striped" style="width: 100%">
        <thead>
        <tr>
            <th>
                Booking Id
            </th>
            <th>
                Date In
            </th>
            <th>
                Date Out
            </th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td style="width: 50%">
                    @obj.BId
                </td>
                <td style="width: 30%">
                    @obj.DateIn
                </td>
                <td style="width: 30%">
                    @obj.DateOut
                </td>
                
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Admin" asp-action="EditBooking"  asp-route-id="@obj.BId"
                           class="btn btn-primary mx-2"> Edit</a>
                        <a asp-controller="Admin" asp-action="DeleteBooking" asp-route-id="@obj.BId"
                           class="btn btn-danger mx-2"> Delete</a>
                        @* <a asp-controller="User" asp-action="Login"  *@
                        @*    class="btn btn-primary mx-2"> Login</a> *@
                    
                    </div>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>